{
  "name": "ignite-ui",
  "private": "true",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  },
  "packageManager": "npm@10.8.1"
}
